type: topograph
version: v0.1
topology:
    nodes:
{% for node in nodes: %}
{{ node }}
{% endfor %}
    links:
    {% for link in links: %}
      - endpoints: [{{link['a']['node']}}:{{link['a']['interface']}}, {{link['b']['node']}}:{{link['b']['interface']}}]
    {% endfor %}